Every node consists of a server and client.
Protocol used : TCP
Steps:
Run all the servers.
Make sure that every node has its own initial knowledge file(config.txt) and the final knowledge file.
then start the client program on all the machines.
once the program stops check the final knowledge file of every node to get the nodes discoverd.
